# Tawk.to live chat — required for the ChatWidget to open a real
# live chat panel. Without these the custom gold launcher still
# renders, but clicking it falls back to a "call us instead" prompt.
#
# How to get them:
#   1. Sign up free at https://www.tawk.to/
#   2. Create a property for Scott Electric Group
#   3. Admin → Channels → Chat Widget → copy the IDs from the embed
#      snippet. The URL looks like:
#      https://embed.tawk.to/<PROPERTY_ID>/<WIDGET_ID>
#
# Copy this file to .env.local and fill in the real values.
NEXT_PUBLIC_TAWK_PROPERTY_ID=
NEXT_PUBLIC_TAWK_WIDGET_ID=

# Resend — required for the Partner & Support contact form (app/actions/contact.ts)
# to actually deliver submissions instead of failing silently.
#
# How to get it:
#   1. Sign up free at https://resend.com/
#   2. Verify the sending domain set in lib/constants.ts (DISCVAULT.emailDomain)
#      under Resend's Domains tab — emails will fail to send until that
#      domain shows "Verified"
#   3. API Keys → Create API Key → copy it here
RESEND_API_KEY=
